# =============================================================
# monitoring/grafana/provisioning/datasources/datasources.yaml
# Auto-provision datasource PostgreSQL saat Grafana start
# =============================================================

apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: btcdb-postgres
    url: postgres:5432
    user: kelompok4_ipbd
    secureJsonData:
      password: k4ipbd_postgres_2026
    jsonData:
      database: btcdb
      sslmode: disable
      maxOpenConns: 10
      maxIdleConns: 10
      connMaxLifetime: 14400
      postgresVersion: 1500   # PostgreSQL 15
      timescaledb: false
    isDefault: true
    editable: false