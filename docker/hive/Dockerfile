# Tidak dipakai lagi — hive-metastore sekarang pakai image langsung di docker-compose.yml
# bitsondatadev/hive-metastore sudah bundle hadoop-aws, postgresql JDBC, dan S3A connector
FROM bitsondatadev/hive-metastore:latest
